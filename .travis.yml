dist: focal
language: python
python:
  - '3.6'
  - '3.7'
  - '3.8'
  - '3.9'

cache: pip

install:
  - pip install -e .
  - pip install pip-tools
  - pip-compile requirements-dev.in
  - pip install -r requirements-dev.txt

script: pytest
